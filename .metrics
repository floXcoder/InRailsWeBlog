require 'metric_fu'

MetricFu::Configuration.run do |config|
  config.configure_metric(:cane) do |cane|
    cane.enabled = true
    cane.abc_max = 20
    cane.line_length = 120
    cane.no_doc = 'y'
    cane.no_readme = 'y'
  end
end
