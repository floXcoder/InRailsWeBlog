'use strict';

const AdminFooterLayout = function () {
    return (
        <footer className="admin-footer">
            {/* Empty for now */}
        </footer>
    );
};

export default AdminFooterLayout;
