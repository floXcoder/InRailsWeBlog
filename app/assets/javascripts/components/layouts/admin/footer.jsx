'use strict';

export default class AdminFooterLayout extends React.PureComponent {
    render() {
        return (
            <footer className="admin-footer">
                {/* Empty for now */}
            </footer>
        );
    }
}
