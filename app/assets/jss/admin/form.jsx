'use strict';

const styles = (theme) => ({
    field: {
        marginLeft: 'auto',
        marginRight: 'auto',
        width: '80%',
        textAlign: 'center'
    },
    errorField: {
        color: theme.palette.error.main
    },
    hint: {
        fontStyle: 'italic'
    }
});

export default styles;
