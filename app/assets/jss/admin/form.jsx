'use strict';

const styles = (theme) => ({
    field: {
        marginLeft: 'auto',
        marginRight: 'auto',
        width: '80%',
        textAlign: 'left'
    },
    errorField: {
        color: theme.palette.error.main
    },
    hint: {
        fontStyle: 'italic'
    }
});

export default styles;
