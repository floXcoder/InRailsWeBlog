'use strict';

import {
    articleWidth
} from '../theme';

const styles = () => ({
    root: {
        position: 'relative',
        margin: '1rem auto 4rem',
        overflow: 'visible',
        maxWidth: articleWidth
    }
});

export default styles;
