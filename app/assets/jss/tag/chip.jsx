'use strict';

const styles = (theme) => ({
    tag: {
        fontSize: '.9rem',
        margin: theme.spacing.unit / 3
    },
    parent: {
        margin: theme.spacing.unit
    },
    child: {
        margin: theme.spacing.unit
    },
});

export default styles;
