'use strict';

const styles = (theme) => ({
    tag: {
        margin: theme.spacing.unit,
    },
    parent: {
        margin: theme.spacing.unit
    },
    child: {
        margin: theme.spacing.unit
    },
});

export default styles;
