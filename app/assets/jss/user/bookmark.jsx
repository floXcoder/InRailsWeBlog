'use strict';

const styles = (theme) => ({
    root: {
        margin: 15,
        minWidth: 220
    },
    title: {
        margin: 5
    },
    link: {
        fontSize: '1rem',
        verticalAlign: 'middle',
        color: theme.palette.primary.dark,
        lineHeight: '1.2rem',
    },
    none: {
        fontStyle: 'italic',
        marginTop: 10
    }
});

export default styles;
