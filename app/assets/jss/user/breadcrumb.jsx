'use strict';

const styles = (theme) => ({
    breadcrumb: {
        margin: '.5rem 3rem',
        position: 'relative'
    },
    chip: {
        margin: theme.spacing.unit,
    },
    currentChip: {
        backgroundColor: theme.palette.background.default,
    }
});

export default styles;
