- provide(:title, t('views.article.show.page_title', title: article.title))

- stylesheet 'articles/show'

- javascript 'commons'
- javascript 'articles/show'

= content_tag :div, '', id: 'article-show-component', data: { article: ActiveModel::SerializableResource.new(article, { scope: current_user }).serializable_hash }
