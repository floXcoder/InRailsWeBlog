if($('#form_flashes').length > 0) {
    $('#form_flashes').remove();
    $('.modal-body').prepend('<%= j(render 'layouts/flash_messages') %>');
} else {
    $('.modal-body').prepend('<%= j(render 'layouts/flash_messages') %>');
}
