p
  = t('email.hello') + ' ' + @resource.pseudo + ','

p
  = t('email.confirmation.instruction')

p
  = link_to t('devise.mailer.confirmation_instructions.subject'), user_confirm_url(confirmation_token: @token)

p
  = t('email.greeting')
